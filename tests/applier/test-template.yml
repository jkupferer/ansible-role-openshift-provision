

apiVersion: v1
kind: Template
labels:
  app: ${APPNAME}
metadata:
  name: test-template
objects:
- apiVersion: v1
  kind: ImageStream
  metadata:
    name: test-template-${APPNAME}
  spec:
    lookupPolicy:
      local: false
parameters:
- name: NAME
  required: true
- name: APPNAME
  required: true
